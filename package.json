{
  "name": "eg-promises-ts",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "start": "./node_modules/.bin/ts-node src/index.ts -w",
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "~2.8.0",
    "gts": "^0.8.0"
  },
  "dependencies": {
    "@types/es6-shim": "^0.31.37",
    "@types/node": "^10.12.0",
    "es6-shim": "^0.35.3",
    "ts-node": "^7.0.1"
  }
}
